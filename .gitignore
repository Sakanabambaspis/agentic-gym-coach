.venv/
__pycache__/
*.pyc
.pytest_cache/
data/
.env

# Raw personal training logs — real data never enters git
/log.md

# Machine-local agent config (absolute home paths)
/.zcode/config.json
# Session-local plan-mode artifacts (harness writes them into the workspace;
# scratch, not repo content)
/.zcode/plans/
